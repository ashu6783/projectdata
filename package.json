{
  "name": "projectdata",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["api", "json-server", "backend"],
  "author": "Your Name",
  "license": "ISC",
  "description": "A project for serving data using JSON Server and Node.js",
  "dependencies": {
    "requireStack": "^0.1.0",
    "cors": "^2.8.5",
    "json-server": "^1.0.0-beta.3"
  }
  
}
